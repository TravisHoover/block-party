# Block Party — working notes

An ad-free iOS block puzzle game (SwiftUI, iOS 17+), built by Travis for his
daughter after the ads in similar games proved unsuitable. No ads, no IAP, no
analytics, no network access — that constraint is the entire point of the app
and should never be traded away for convenience.

## Layout

| Path | What it is |
| --- | --- |
| `BlockParty/GameEngine.swift` | All rules: board state, placement, line clearing, scoring, combos, game over |
| `BlockParty/PieceLibrary.swift` | Piece shapes, `BlockColor` palette, weighted random generation |
| `BlockParty/GameView.swift` | Main screen, drag-and-drop handling, tray, settings sheet |
| `BlockParty/BoardView.swift` | 8×8 grid, block rendering, ghost preview, clear animations |
| `BlockParty/GameOverView.swift` | End-of-game overlay |
| `BlockParty/Haptics.swift` | Haptic feedback wrappers |
| `marketing/` | App Store listing copy + screenshots |
| `.github/workflows/release.yml` | Tag → build → sign → upload to App Store Connect |

Open `BlockParty.xcodeproj` in Xcode. There is no test target and no PR CI —
the release workflow only runs on `v*` tags or manual dispatch.

## Conventions

- Work on a branch, open a PR. Travis reviews before merging; don't push to `main`.
- Don't create a PR unless the work is actually finished.
- Comments explain *why*, not *what*. Match the existing density.

## Hard-won gotchas — read before touching these areas

**Drag and drop.** Placement is computed in **global screen coordinates**, and
the board's frame reaches `GameView` by writing directly to a `@Binding` from
the grid's own `GeometryReader` (`onAppear` + `onChange`). An earlier version
used a named coordinate space, and another used a `PreferenceKey` — **both
silently failed on device**, leaving the board frame unset so every drop was
rejected while working fine in the simulator. Do not reintroduce either.
Grid math also derives cell size from the *measured* frame, not the computed
layout value, so it can't drift from what's rendered.

**`BlockColor` raw values are persisted** (once PR #8 lands, in saved games).
Never reorder or remove cases; only append.

**CI must use the latest stable Xcode.** Apple periodically raises the minimum
SDK for App Store submission and rejects older builds at the *export* step with
a confusing "SDK version issue" message. The workflow uses
`maxim-lobanov/setup-xcode@v1` with `latest-stable` rather than a pin.

**`ASC_API_KEY_P8` is plain text, not base64.** Apple's `.p8` is already PEM
(`-----BEGIN PRIVATE KEY-----`). Base64-encoding it into the secret caused
`keyPathInvalid`. `DIST_CERT_P12_BASE64` *is* binary and does need encoding.

**The App Store Connect API key needs the Admin role.** With App Manager,
`-exportArchive` fails with "Cloud signing permission error / No profiles
found" because automatic signing can't create provisioning profiles.

**Asset generators are gone.** The icon, sound effects, and marketing
screenshots were generated by throwaway Python/Playwright scripts in an
ephemeral scratchpad that has since been wiped. The committed outputs are the
source of truth. Sounds are synthesized bell/sweep tones stored as
asset-catalog **data sets** (loaded via `NSDataAsset`, so adding one needs no
project-file resource changes). For new screenshots, prefer the real iOS
Simulator (⌘S) over recreating the HTML mock.

## Release process

1. Merge to `main`.
2. Bump `MARKETING_VERSION` in `BlockParty.xcodeproj/project.pbxproj` if the
   store version is changing. The build number comes from the CI run number.
3. `git tag v1.2.3 && git push origin v1.2.3` (or run the workflow manually).
4. ~10 min later the build appears in App Store Connect → TestFlight.
5. Attach it to the version page and submit.

Required repo secrets are documented in the README's publishing section.

---

# Current state — pick up here

_Last updated: 2026-08-15, at the end of a **local** session that read App Store
Connect directly in a browser and compiled the app for the first time outside
Xcode. It supersedes an earlier web-session note that guessed at review state
and got most of it wrong — see "Corrections" below._

## App Store status — verified in App Store Connect on 2026-08-15

App: **Block Party! Kids Puzzle**, Apple ID `6788764186`. Store status is
**Rejected**, not "in review". One submission exists (`a7849883`), opened
2026-07-16, now showing **Unresolved Issues**.

The whole review history is one argument about whether this is a children's
app, and it is now settled — correctly:

| When | Who | What |
| --- | --- | --- |
| Jul 14 | Apple | **2.3.8 Accurate Metadata** — name contains "Kids" but the app wasn't submitted as a Kids app. Fix by removing the kid terms *or* selecting Made for Kids. |
| Jul 14 | Travis | Replied that he'd set Made for Kids, under 5. |
| Jul 15 | Apple | **4.1(a) Copycats** + **4.3(a) Spam** — metadata referenced Block Blast. |
| Jul 16 | Travis | Resubmitted with metadata scrubbed (PR #7). This is build 5. |
| Aug 4 | Apple | **2.1 Information Needed** — the four Guideline 1.3 kids questions. |
| Aug 8 | Travis | **Sent the prepared reply.** Already done; don't resend. |

**Made for Kids is correctly configured.** Age Ratings step 7 reads "Ages 4+,
Made for Ages 5 and under". Do not be misled by the Category dropdown showing
Games → Puzzle / Family: modern App Store Connect expresses the Kids category
through the **Age Ratings override**, not the category list ("Kids" isn't even
an option there). The Jul 14 2.3.8 issue is resolved.

Note the warning on that screen: **Made for Kids cannot be undone once the app
is approved.** It is still changeable while unapproved.

The app is genuinely clean for Guideline 1.3, verified by reading the source:
no `URLSession`, no links out, no web views, no GameKit, no IAP, no third-party
SDKs. Imports are only SwiftUI, UIKit, AVFoundation. `UserDefaults` holds the
high score, the sound toggle, and the saved board. App Privacy is "Data Not
Collected"; the privacy policy URL is live and public and has a children's
section.

## The one thing actually blocking release

`Resubmit to App Review` on the submission page is **greyed out**, and the app
has sat untouched for a week because of it. Replying to a 2.1 message does
**not** put the app back in the queue — the earlier note claimed it did, which
is what cost that week.

Best read of the disabled button (inferred, not stated by Apple): **nothing has
changed since the rejection**, so there is nothing to resubmit. `Update Review`
on the version page *is* enabled. Make a real edit to the version, save it, and
resubmission should unlock. No new build is needed and none should be uploaded —
build 5 is what Apple has been reviewing.

## Outstanding

1. **Replace the uploaded screenshots** — this is both a genuine fix and the
   edit that should unblock resubmission. Three are live on the 6.5" slot, and
   the "New Best" one **shows confetti, which build 5 does not have** (that's
   PR #8). That is a real Guideline 2.3.3 accuracy problem, and metadata
   accuracy gets more attention on a Made for Kids app. All the committed
   screenshots are HTML mocks, not real captures — the live app's Classic theme
   is a much brighter violet than any of them. Capture real ones from build 5's
   code (commit `69267a5`), not from `main`.
2. **Then resubmit** and reply noting the Made for Kids designation is set and
   the Aug 8 answers stand.
3. **1.1 = the polish pack, already on `main`.** `MARKETING_VERSION` is still
   `1.0` and no tags exist, so nothing has shipped. Don't tag until 1.0 is
   approved; ship 1.1 with themed screenshots so binary and listing match.
4. **Bundle ID is `com.example.BlockParty`** — already registered under that
   name, so it stays, but it looks like a placeholder.

## Corrections to the previous handoff note

Recorded because these wrong facts each sent a session down a wrong path:

- The first rejection was **2.3.8 (the "Kids" name)**, not 4.1(a) Copycats.
  Copycats came second, alongside a **4.3(a) Spam** citation the old note
  omitted entirely.
- The 2.1 reply was **already sent on Aug 8**. It was listed as the top
  outstanding task.
- The app was **not** "in review" — it was Rejected and idle.
- **PR #8 was already merged** to `main` (`f3ffb26`) when the note said to hold
  it as unmerged. It merged four minutes before the note's own PR.
- "A 2.1 hold is cleared by replying, not resubmitting" is **half wrong**: no
  new *build* is needed, but a resubmission is.

## Local environment

- `xcode-select` points at **CommandLineTools**, so `xcodebuild` and the
  simulator tooling fail by default. Xcode 26.6 is installed. Prefix commands
  with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` to build and
  drive `simctl` without sudo. Tools needing a real developer dir (the native
  simulator panel, tap/drag injection) still require
  `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`, which needs
  Travis's password.
- **The app builds clean** for the simulator, first time it has been compiled
  outside Xcode: `xcodebuild -scheme BlockParty -sdk iphonesimulator`.
- Still unverified on device: drag-and-drop placement, save/resume across a
  force-quit, the mute toggle, theme switching. All need input injection, hence
  the sudo command above.
